main: me.bryangaming.labority.Labority
name: Labority
version: ${project.version}

api-version: 1.13

depend:
  - Vault

commands:
  jobs:
    description: Main command.
    aliases:
      - labority
      - labor

    usage: /jobs help.